export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
  EXAM_MANAGER = 'EXAM_MANAGER',
}

export enum CandidatureStatus {
  INDETERMINATE = 'INDETERMINATE',
  ACCEPTED = 'ACCEPTED',
  REJECTED = 'REJECTED',
}
